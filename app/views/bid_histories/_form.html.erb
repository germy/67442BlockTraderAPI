<%= form_for(@bid_history) do |f| %>
  <% if @bid_history.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bid_history.errors.count, "error") %> prohibited this bid_history from being saved:</h2>

      <ul>
      <% @bid_history.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :order_id %><br>
    <%= f.number_field :order_id %>
  </div>
  <div class="field">
    <%= f.label :price %><br>
    <%= f.text_field :price %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
